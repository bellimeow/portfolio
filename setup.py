from setuptools import setup

setup(
    name='TDP003-Portfolio',
    packages=['myFlaskProject'],
    url='http://www.python.org/sigs/distutils-sig/',
    include_package_data=True,
    install_requires=[
        'flask',
    ],
#      install_requires=['Django>=1.3'],
     )
